# 하남 미사 가족 나들이 큐레이션 (재구축판)

Antigravity(Gemini) 세션에서 만든 1차본은 **근거 없는 생성 데이터(팬텀 데이터)** 가 대부분이라 폐기하고,
실제 유튜브·네이버 자료에 근거해 다시 만든다. 1차본 원본은 `../antigravity_원본백업_여행/`, 세션 기록은 `../antigravity_session/`.

## 절대 규칙 — 데이터 무결성
1. **출처 없는 사실은 쓰지 않는다.** 모든 장소·가격·시설(입식/무장애/계단 등) 정보는 `source_refs`로
   실제 영상(ID+타임스탬프) 또는 실제 블로그 글 URL을 가리켜야 한다. 없으면 필드를 비우거나 `"unverified"`로 표시하고 UI에 "확인 필요"로 노출.
2. **유튜브 링크 = 실제 영상 URL(`watch?v=ID&t=초`)만.** `youtube.com/results?search_query=…` 같은 검색 URL을 "참고 영상"으로 넣지 않는다.
   네이버도 동일: 실제 글 URL. 검색 URL은 "더 찾아보기" 라벨로만 별도 표기.
3. **좌표·주소·전화는 지도 API에서 받은 값만.** 손으로 만든 위도/경도 금지(1차본의 `37.5212,127.2341` 류가 그 사례).
   장소 링크는 카카오 place id 기반(`https://place.map.kakao.com/{id}`)을 우선.
4. **소요시간은 계산값만.** 집 좌표 → 목적지 좌표를 OSRM(또는 카카오모빌리티)으로 계산해 `drive_min`에 저장. 눈대중 금지.
5. **확신 없으면 후보로 둔다.** `"A or B"` 같은 모호한 항목 금지. 검증되면 `verified_at` 기록.
6. 완료 보고 전 `tools/verify.py`(링크·좌표·필수필드 검증)를 돌리고 결과를 그대로 보고한다.

## 가족 프로필 (기존 GEMINI.md 계승)
- 출발지: 경기도 하남시 미사강변중앙로 120 (선호 1시간 이내, 최대 1시간 30분)
- **모드 A 4인**: 아빠(88), 엄마(91), 아들(17년생·초등), 딸(21년생·유아). 자연체험·동물 먹이주기·숲놀이터·안전한 체험. 식비 4인 한 끼 10만원 이하
- **모드 B 6인(3대)**: 4인 + 어머니(60), 아버지(55, **파킨슨병**). **휠체어는 타지 않고 걸으심**(휠체어 접근성은 기준 아님). 핵심은 낙상 위험 요소 배제:
  계단·급경사·미끄럽거나 울퉁불퉁한 길·긴 보행거리, 주차→목적지 도보 최소, 앉아 쉴 곳(벤치·그늘), 식당 **전 좌석 입식**, 부드러운 음식
- 식당 1·2·3순위 / 카페 1·2순위 다중 옵션, 테마: 동물교감·숲속힐링·바다리버뷰·공예수확·캠핑피크닉·키즈특화
- 결과물은 **스마트폰에서 바로 열리는 웹**(GitHub Pages `kimbap64-cell.github.io/family-trip`) + 부부 공유 + 카톡 알림
- 사용자는 명령어를 직접 다루지 않는다 → 자동화 필수

## 실행 환경 (이 PC = 가족 메인 PC)
- Python: `%LOCALAPPDATA%\Programs\Python\Python312\python.exe` (PATH 미등록. 스크립트는 전체경로로 호출)
- 설치됨: yt-dlp, youtube-transcript-api, requests, beautifulsoup4, lxml
- ⚠️ 스크래치 스크립트 이름을 `inspect.py` 등 표준 라이브러리 이름으로 만들지 말 것(모듈 가림)
- 비밀정보(`kakao_token.json`, `.env`, API 키)는 gitignore. 채팅/로그/커밋에 값 출력 금지
- **원본 `Desktop\antigravity\여행`은 수정하지 않는다**(Antigravity가 실행 중이라 덮어쓸 수 있음). 작업은 이 사본에서.
- 푸시(GitHub Pages 반영)는 사용자 승인 후에만

## 수집 예절 (⚠️ 2026-09-24에 집 IP가 구글에 차단된 사고 있음)
yt-dlp로 영상 상세를 150건 가까이 빠르게 조회 → 구글이 **집 공인IP(가족 전체)에 "비정상 트래픽" 캡차**를 걸었고
yt-dlp·youtube-transcript-api 모두 `Sign in to confirm you're not a bot` / `IpBlocked`. 캡차는 사람이 푸는 것이며 **우회 금지**.
- 유튜브 요청은 **동시 1개, 요청 간 ≥20초, 하루 ≤100건**. 차단 신호(`not a bot`, `IpBlocked`, 캡차 페이지) 1회 감지 시 **즉시 전체 중단**하고 사용자에게 보고. 재시도 루프 금지.
- 이미 받은 것은 `data/cache/`, `data/sources/`에 저장돼 있으니 **재요청 금지**(캐시 우선).
- 네이버 지도 통합검색 API(`map.naver.com/p/api/search/allSearch`)는 ncaptcha로 막혀 있음 → 쓰지 않는다.
  공개 플레이스 페이지(`pcmap.place.naver.com/place/list`, `m.place.naver.com/place/{id}/home`)의 `__APOLLO_STATE__`는 응답 확인됨. 요청 간 ≥2초, 소량만.
- 공식 API가 있는 것은 공식 API 우선(유튜브 Data API v3, 네이버 오픈API, 카카오 로컬).

## 외부 서비스 현황
| 서비스 | 상태 |
|---|---|
| 유튜브 Data API v3 | **키 발급·검증 완료**(프로젝트 family-trip-509603, 키는 YouTube Data API v3로만 제한, `.env`의 YOUTUBE_API_KEY). 영상 통계·댓글수·구독자·댓글은 이걸로 |
| 유튜브 검색·메타(yt-dlp) | 동작했으나 **현재 IP 차단 중** → API로 대체, 자막만 차단 해제 후 저속 |
| 유튜브 자막 | youtube-transcript-api, **현재 IP 차단 중** |
| 네이버 플레이스 | 별점(방문자 5점만점)·리뷰수·좌표·편의시설(주차)·영업시간·메뉴·블로그후기 링크 추출 확인(화담숲 id 35708336 → 4.49, 11,137건) |
| 네이버 검색 오픈API | **사용 안 함.** 2026-07-31부로 개발자센터 신규 신청 종료 → NAVER API HUB(NCP 계정 별도 필요)로 이전(외부 블로그 기준, 공식 미확인). 필요 없음: 플레이스 상세(`m.place.naver.com/place/{id}/home`)에 **장소별 최근 블로그 후기(URL·작성일·본문발췌)와 실제 입장료/메뉴 가격**이 이미 있음 |
| 구글 Places | **당분간 미사용(사용자 결정 B, 2026-09-24).** 쓴다면 용도는 ①네이버 별점이 없는 곳의 별점 보완 ②폐업 이중확인 **두 가지뿐**(필드마스크로 rating/userRatingCount/businessStatus만). 재개 조건은 파일럿 후 별점 공백이 실제 문제일 때 |
| 구글 결제 | 계정 `014B9A-CC54E1-124E0A` = **무료 체험판**(크레딧 ₩376,672 남음, 2026-11-26 종료, 업그레이드 안 하면 청구 없음). **family-trip-509603은 이 계정에 자동 연결돼 있음**(다른 프로젝트 3개도 연결). family-trip에 켠 API는 YouTube Data API v3(무료)뿐. 유료 API 활성화·유료 계정 업그레이드 금지(사용자 명시 승인 전) |
| 카카오 로컬 | **2026-09-24 활성화·검증 완료**(앱 가족나들이 ID 1587118, 카카오맵 ON, 이 앱이 일간 무료쿼터 대상). 주소→좌표, 키워드 검색(장소ID·place_url·주차장 좌표). REST 키는 `kakao_token.json` |
| 카카오 '나에게 보내기' | 토큰 발급 완료(`kakao_token.json`, gitignore). `send_kakao_alert.py` 유지, 새 데이터 구조에 맞춰 개편 예정 |
| 경로/시간 | OSRM 공개서버 동작 확인 (집→아인티오 좌표 23분 등) |

## 🔒 API 안전캡 (돈이 나가면 안 됨 — 절대 규칙)
- **모든 외부 호출은 `tools/quota_guard.py`를 거친다.** 새 도구를 만들 때도 `charge()`/`require_google_api()` 없이 외부 API·스크래핑을 호출하지 않는다.
- 한도는 `config/limits.json`(일 3,000 유닛·검색 40회 등). 사용자 승인 없이 올리지 않는다. 유튜브 스크래핑 캡은 0.
- `family-trip` 프로젝트에는 **유료 가능 API(Places/Maps/Gemini 등)를 켜지 않는다.** 결제 연결은 사용자 지시로 유지 중이지만 체험판 상태에서만, 사용자 승인 후에만 유료 API를 켠다.
- 시험: `python tools/test_quota_guard.py` (17개 통과 상태 유지). 설명: docs/API_SAFETY.md

## 확정된 결정 (사용자, 2026-09-24)
- API: **유튜브 Data API + 네이버 오픈API(검색) + 카카오 지도(로컬) 활성화 + 구글 Places(폐업·별점, 결제수단은 사용자가 직접 입력·상한/예산알림 필수)**. 키는 Claude가 발급·저장, 로그인/캡차/결제/본인인증은 사용자
- 노출 단위: **장소를 검증·점수화한 뒤 지역별 코스로 조합**
- 갱신: **가능한 한 전부 자동화**. 리스크(차단·오판독)가 있는 구간은 Claude가 판단해 주기·범위를 조절. **폐업 여부 확인은 필수 기능**
- 라이브 사이트: 팬텀 데이터를 내리고 **'재구축 중' 안내로 교체**(푸시 승인됨)
- 우선순위: 당일 나들이 코스 → 동네(도보·차량 10분) 맛집·카페(모수 확대) → 키즈캠핑

## 자동화 정책 (계층형)
| 계층 | 하는 일 | 주기 | 실행 위치 | 자동 게시 |
|---|---|---|---|---|
| 1 상태 점검 | 네이버 플레이스+구글 Places로 별점·리뷰수·**폐업/휴업** 재확인, 링크 점검 | 주 1회(폐업은 화이트리스트 장소만) | GitHub Actions | 예 — 폐업 감지 시 즉시 강등/제외 + 카톡 알림 |
| 2 신규 발굴 | 유튜브 API 검색 → 신규 영상 → 자막/후기 판독 → 후보 생성 | 월 1~2회 | Claude 예약 실행 또는 요청 시 | **모든 게이트 통과 + 독립 출처 2개 이상 + 장소 ID 확정**인 것만 자동 게시, 나머지는 '후보 대기' + 카톡 요약 |
| 3 알림 | 이번 주말 추천/변경 요약을 카카오 나에게 보내기 | 주 1회(금) | Actions | 예 |
- 원칙: 차단 신호(캡차·IpBlocked) 1회에 해당 소스 자동 중단 + 카톡으로 보고, 다음 주기까지 재시도 금지. 자동화가 실패하면 **조용히 낡은 데이터를 유지하지 말고 사이트에 '마지막 확인일'을 표시**

## 저장소·협업 정책
- **GitHub `kimbap64-cell/family-trip`(public, Pages 활성)가 원본.** 이 PC 파일에 의존하지 않고 어디서나 이어서 작업 가능하게 유지. 가족 프로젝트지만 비공개 필요는 없다는 것이 사용자 의사.
- 그래도 **비밀(카카오 REST키·토큰·OAuth code)은 절대 커밋 금지.** 세션 원본(`../antigravity_session/raw/`)에 키가 들어 있으므로 올리지 않는다(마스킹된 `.md`만).
- 커밋·푸시는 사용자 확인 후. 푸시하면 라이브 사이트(Pages)가 바뀐다.
- 사용자는 명령어를 다루지 않는다. 작업은 Claude가 수행하고 결과는 웹/카톡으로 전달.
- 1차본의 팬텀 파일(index.html, trips_data.*, mobile_guide.md, auto_collector.py, weekly_update.yml 등)은 삭제 확정(사용자 승인). 원본은 `../antigravity_원본백업_여행/`과 git 이력에 있음.

## 이력·앱 (2026-09-26)
- **목록은 지우지 않는다.** 장소는 `data/registry.json`(이력 저장소)에 영구 기록되고 변화(새로 추가·등급/별점·이름변경·빠짐·폐업)는 날짜 이벤트로 남는다. 설계·한계: docs/UPDATE_PLAN.md. 새 장소는 '검토 대기'로만 보이고 근거를 읽어 점수화한 뒤에야 추천된다.
- 사이트는 PWA(홈 화면 추가): manifest.webmanifest · sw.js · icons/. 사이트 구조를 바꾸면 sw.js의 CACHE 이름을 올릴 필요는 없다(최신 우선 방식).
- 키즈캠핑은 화장실·샤워실이 핵심 기준(docs/SCORING.md). 캠핏·땡큐캠핑 가격은 이름·지역 일치가 안 돼 데이터로 쓰지 않는다.
