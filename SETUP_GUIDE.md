# 📱 카톡 알림 연동 & 부부 스마트폰 공유 초간단 세팅 가이드

컴퓨터 명령어를 다루기 어려우신 분들을 위해, **클릭 몇 번으로 아내분과 함께 쓰고 카톡 알림까지 받는 방법**을 가장 쉽게 정리했습니다.

---

## 1. 아내분과 함께 스마트폰에서 접속하는 방법

### (방법 A - 지금 당장 내 폰에서 바로 열어보기)
1. 지금 폴더 안에 있는 `index.html` 파일을 더블클릭하면 PC 브라우저(크롬, 엣지 등)에서 바로 열립니다.
2. 개발자 도구(`F12`)를 누르고 `Ctrl+Shift+M`을 누르면 스마트폰 모바일 화면으로 테스트해 보실 수 있습니다.

### (방법 B - 남편 & 아내 스마트폰에 영구 주소 만들기: 3분 완성, 무료)
컴퓨터가 꺼져 있어도 두 분이 언제 어디서든 볼 수 있는 방법입니다:
1. **GitHub.com**에 무료 회원가입 후 새 저장소(New Repository, 이름: `travel-guide`)를 만듭니다.
2. 지금 폴더의 `index.html`과 `trips_data.json` 두 파일을 마우스로 드래그해서 올립니다.
3. 저장소의 **[Settings] ➔ [Pages]** 메뉴에서 `Branch: main`을 선택하고 **[Save]**를 누릅니다.
4. 1분 뒤 **`https://내아이디.github.io/travel-guide`** 라는 두 분만의 인터넷 주소가 생성됩니다!

---

## 2. 스마트폰 바탕화면에 '우리 가족 나들이' 앱 아이콘 만들기

부부의 스마트폰에서 위 웹 주소로 접속한 후:
- **아이폰 (사파리 Safari)**:
  - 화면 하단 가운데 **[공유(네모에 위쪽 화살표 ↑)]** 버튼 터치
  - 메뉴에서 **[홈 화면에 추가]** 터치 ➔ 이름 확인 후 **[추가]**
- **갤럭시 (크롬 Chrome / 삼성인터넷)**:
  - 화면 우측 상단 **[점 3개 ⋮]** 터치
  - 메뉴에서 **[홈 화면에 추가]** 터치 ➔ **[추가]**

> 이제 앱스토어에서 앱을 다운받은 것처럼, 스마트폰 바탕화면의 아이콘만 누르면 즉시 나들이 가이드가 열립니다!

---

## 3. 카카오톡 '나에게 보내기' 알림 연동 (1회만 설정)

새로운 추천 코스가 업데이트될 때 내 카카오톡으로 자동 알림을 받으시려면 카카오 개발자 센터에서 키를 한 번 발급받아야 합니다.

1. **[카카오 개발자 사이트(developers.kakao.com)](https://developers.kakao.com)** 에 접속하여 카카오톡 계정으로 로그인합니다.
2. 상단 **[내 애플리케이션] ➔ [애플리케이션 추가하기]** 클릭 (앱 이름: `가족나들이`, 사업자명: `개인`)
3. 생성된 앱의 **[앱 키]** 메뉴에서 **`REST API 키`**를 복사합니다.
4. 좌측 **[카카오 로그인]** 메뉴에서 상태를 **[활성화(ON)]**로 켭니다.
5. 좌측 **[카카오 로그인] ➔ [동의항목]**에서 **`카카오톡 메시지 전송 (나에게 보내기)`** 항목을 **[이용 중 동의]**로 설정합니다.
6. 상단 **[도구] ➔ [REST API 테스트]**에서 `카카오톡 메시지 전송` 토큰을 발급받아 복사합니다.
7. 지금 폴더의 `kakao_token.json` 파일을 메모장으로 열고 다음과 같이 붙여넣습니다:
```json
{
  "rest_api_key": "복사한_REST_API_키",
  "access_token": "발급받은_액세스_토큰",
  "refresh_token": "발급받은_리프레시_토큰"
}
```

---

## 4. 컴퓨터 조작 없는 자동 업데이트 방법

### 1) PC에서 수동으로 업데이트하고 싶을 때 (명령어 전혀 불필요!)
- 폴더 안에 만들어 드린 **`원클릭_나들이업데이트.bat`** 파일을 마우스로 **더블클릭**만 하시면 됩니다.
- 알아서 최신 여행지가 DB에 추가되고, 카카오톡 알림이 발송됩니다.

### 2) PC를 아예 켜지 않아도 돌아가는 완전 무인 자동화 (GitHub Actions)
- GitHub 저장소의 `Actions` 탭에 매주 금요일 아침 8시 자동 실행 스케줄(Cron)을 걸어두면, **집에 컴퓨터가 꺼져 있어도 클라우드가 알아서 매주 금요일마다 새로운 나들이 코스를 발굴해 웹페이지를 갱신하고 카톡을 보내줍니다.**
