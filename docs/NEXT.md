# 재개용 인수인계 (2026-09-24 저녁 기준)

새 세션에서 이 저장소를 열면 **CLAUDE.md → 이 문서** 순으로 읽고 이어서 하면 된다.

## 지금 라이브인 것 — https://kimbap64-cell.github.io/family-trip/
- 탭 1 **동네 식당·카페** 45곳(집 기준 차량 12분): 추천 10 · 조건부 18 · 근거부족 14 · 제외 3
- 탭 2 **당일 나들이** 30곳(편도 90분): 추천 10 · 조건부 15 · 근거부족 4 · 제외 1. 카드에 **가기 전 확인(예약·휴관일)**, 근처 식당 1·2·3순위·카페 1·2순위(간이 검증)
- 4인/6인(부모님) 토글, 근거 링크, 네이버 내비 버튼. 사람이 검토하는 표: docs/PILOT_MISA.md, docs/DAYTRIP.md

## 전체 재생성 (캐시 우선, 외부 호출 거의 0)
```
python tools/run_all.py        # 캐시 재추출 -> 점수화 -> 코스 -> 사이트 -> verify
python tools/verify.py         # 게시 전 필수 (오류 0이어야 푸시)
python tools/test_quota_guard.py
```
개별: pilot_local.py(동네 수집) · daytrip_collect.py(나들이 수집) · pilot_youtube.py(유튜브 API, 캐시 있으면 0회) · pilot_score.py · daytrip_courses.py · build_site.py · show_places.py / show_evidence.py(판독 검증용)
파이썬: `%LOCALAPPDATA%\Programs\Python\Python312\python.exe`. 키는 `.env`. 커밋 메시지는 파일로 저장 후 `git commit -F`.

## ✅ 2026-09-26 진행 결과
- **자동 점검 첫 스케줄 실행 성공**(9/25 08:11 KST, 실행 #4): 115곳 점검, 별점 변동 0·폐업 의심 0·실패 0, 봇 커밋 511b521.
- **버그 2건 발견·수정(재발 방지 시험 포함)**
  1. 캠핑 별점 0.0 덮어쓰기: 네이버 캠핑 목록은 별점이 없으면 `0.0`을 주고 실제 별점은 상세 페이지에만 있다. collect_one 이 목록값을 저장해 봇이 고친 별점(4.84 등)을 다시 0.0으로 덮어써 캠핑 등급이 과소평가(별점 없음→카카오 보정) → 목록 0.0=None 처리 + **상세 별점 우선**. `test_collect_rating.py`(5개)가 워크플로 시험에 포함.
  2. run_all 이 pilot_youtube 로 youtube.json 을 새로 쓰면서 장소별 유튜브 검색 결과를 덮어씀 → run_all 에 `yt_place_search`(캐시 우선, 새 장소만 API) 병합 + 재점수화 단계 추가.
- **유튜브 근거 확대(장소별 맞춤 검색)**: `tools/yt_place_search.py` — 추천·조건부 상위 장소마다 '<이름> 후기/캠핑' 검색 → 제목/설명에 이름이 있는 영상만 채택(영상 신뢰도 S·타임스탬프 링크). 나들이 12곳·캠핑 12곳, API 검색 24회(캐시 `data/cache/ytps`).
- 결과: 동네 45(추천 10) · 나들이 45(추천 15·조건부 22) · 캠핑 25(추천 6·조건부 14·근거부족 5·제외 0, 오토캠핑 15곳 포함).
- 남은 일: ① 동네 식당·카페에도 장소별 유튜브 검색 적용 ② 나들이 근거부족 6곳 보강 ③ 캠핑 요금 실제 예약가 확인 ④ 동네 범위 확대 ⑤ 카톡 8시 알림 도착 확인(사용자)

## ✅ 2026-09-25 새벽 진행 결과 (아래 '내일 할 일' 1~4 완료)
- 캠핑 25곳(오토캠핑 15 포함) 블로그 읽기·점수화 완료 → 추천 4 · 조건부 5 → **캠핑 탭 공개**(유형 필터: 전체/오토캠핑/글램핑·카라반). 히든백야드(미사 5분, 당일이용)·사노리숲·블루문·양주 캠프랩·무수아취가 오토캠핑 추천/조건부.
- 나들이 45곳으로 확대(추천 15 · 조건부 22). run_all 의 daytrip_collect 가 `--top 30` 으로 고정돼 있어 45→30 으로 덮어쓴 실수를 발견해 45로 수정.
- 인용문 검증으로 오판 추가 수정: 사노리숲 '바닥에 앉을 공간 부족/책상 테이블'을 좌식으로 오판 → 입식으로 정정, '트램플린'이 '트램'(이동수단)으로 오판 → 제외.
- 남은 일: ① 나들이 '근거부족' 6곳·캠핑 '근거부족' 15곳 블로그 보강(내일 캡) ② 유튜브 근거가 얕음(나들이 11곳·캠핑 1곳만 언급) → 영상 설명·댓글 검색 확대(유튜브 API 검색 40회/일 안에서) ③ 캠핑 요금은 네이버 등록가/최저 사이트 요금 추정 — 실제 예약 요금 확인 필요 ④ 동네 확대 ⑤ 자동 점검 결과(8시) 확인

## (이전) 내일(캡 리셋, 2026-09-25 00:00 KST 이후) 바로 할 일 — 순서대로
오늘(9/24) 블로그 읽기 캡 150/150 소진 → 아래 명령은 **캐시 덕에 오늘 이미 받은 건 다시 안 받고**, 블로그 본문·카카오 패널만 새로 채운다.
1. `python tools/camp_collect.py --top 25 --blogs 2` — 키즈캠핑 후보의 **블로그 본문 읽기(캠핑은 오늘 0편)**·카카오 패널 보강
2. `python tools/daytrip_collect.py --top 45 --blogs 2` — 나들이 후보 45곳으로 확대 + 광고 아닌 글 2편 미달 장소 보강 (블로그 캡 150 안에서 캠핑 다음)
3. `python tools/pilot_local.py --blogs-per-place 2` 는 동네용(이미 대부분 채움). 필요 시만
4. `python tools/run_all.py` → 결과 표(docs/CAMPING.md·DAYTRIP.md)에서 **인용문을 직접 읽어 판정 검증**(show_evidence.py). 판독 오판이 여러 번 있었다.
5. 블로그 캡이 또 모자라면 다음 날로 이어서(같은 명령 재실행) — 캐시가 진행 상태를 보존한다.
(참고) 카카오 패널 캡은 **일 150·월 2,500으로 상향됨(사용자 승인, 위험은 돈이 아니라 IP 차단)**. 캠핑 25 + 나들이 45 + 동네 45 = 115 → 하루에 수용. 403/429 첫 신호엔 즉시 중단. 주간 Actions 점검도 카카오 캡 도달 시 네이버만 점검하고 계속 진행하도록 완화돼 있음.

## 그 외 남은 일
4. **카톡 알림**: `tools/notify_kakao.py` 완성(기본 미리보기, `--send`는 사용자 승인 후). 주간 추천은 네이버 별점이 있거나 카카오 원점수 ≥4.3인 '추천'만. **시험 발송 승인 대기.** 남은 것: 주 1회 상태 점검 자동화 방식 결정(로컬 예약 / Actions / 요청 시 실행) — Actions는 네이버 공개페이지·카카오 패널이 클라우드 IP에서 막힐 수 있고 Secrets 등록이 필요
5. 동네 확대(반경·카테고리), 유튜브 자막(IP 차단 해제 확인 후 저속, youtube_scrape 캡 승인 필요)

## 이번에 배운 것 (재발 방지)
- **네이버 캠핑·숙박 검색은 목록 형식이 다르다**: `○○ 캠핑장` → `AccommodationSearchItem`(placeReviewScore·placeReviewCount·좌표·`matchRoomMinPrice` 최저 사이트 요금). `○○ 오토캠핑장`은 0건. list_places 가 두 형식을 모두 파싱한다.
- Actions 첫 실행에서 발견: 비어 있는 값(None)에 죽는 버그(test_score_robust 로 재현·방지), 클라우드 카톡은 access_token 이 빈 채로 시작해 재발급부터 해야 함(test_notify_flow 로 방지).
- **정규식 판독은 자주 틀린다**: 부분시설 폐업·추측성 폐업, "엘리베이터나 계단", "많이 걸었는데"(본인 산책), "걷기 힘든 어른도 좋아요"(긍정), 온라인 예약 대기번호(웨이팅), 전기차 요금(이동수단), 그늘 주차(쉼터) 등을 오판했고 패턴·문맥 규칙으로 고쳤다. **점수·판정은 반드시 인용문을 읽어 표본 검증.**
- 근거 수집 부족(광고성 글만 읽음)은 '제외'가 아니라 '근거부족'. 입장료 4인 15만원 초과는 추천 상한 조건부.
- 카카오 별점은 네이버보다 평균 0.8 낮고 표본이 작다 → 네이버 별점 없을 때만 +0.8 보정·기준 0.3 완화(화면 표시).
- 큰 공원은 네이버/카카오 좌표가 수백 m~1km 다를 수 있어 나들이는 매칭 허용 1.5km.
- 국립수목원 휴관 요일이 글마다 월/화로 엇갈림 → 카드에 "글마다 달라요, 공식 확인" 경고를 자동 표시.

## 한계 (숨기지 않는다)
- 네이버 내비 `nmap://navigation` 딥링크: **사용자 폰 실기기에서 정상 동작 확인(2026-09-24)**. 폴백(네이버 지도 웹 링크)은 유지.
- 코스의 식당·카페는 **간이 검증**(네이버 목록 별점·리뷰 + 카카오 존재). 메뉴·좌석·블로그 본문은 미확인.
- 유튜브 근거는 API 검색 8쿼리 결과 중 장소가 언급된 곳만(나들이 8곳). 자막은 IP 차단으로 미사용.
- 시범판은 유명 장소 위주(리뷰 많은 곳 우선 정렬) → 숨은 명소는 아직 약함.

## 주의
- 돈이 나가면 안 됨: 모든 외부 호출은 quota_guard 경유(docs/API_SAFETY.md). 오늘(9/24) 사용: 유튜브 1,644/3,000유닛·검색 16/40, 네이버 장소 163/400, 블로그 150/150(소진), 카카오 로컬 125/300, 카카오 패널 70/100.
- 집 IP 구글 캡차 차단 중(유튜브 스크래핑 금지). 네이버는 공개 페이지만 저속·소량.
