# 재개용 인수인계 (2026-09-24 기준)

새 세션에서 이 저장소를 열면 **CLAUDE.md → 이 문서** 순으로 읽고 이어서 하면 된다.

## 지금 라이브인 것
- https://kimbap64-cell.github.io/family-trip/ — **우리 동네(집 기준 차량 12분) 식당·카페 시범판 45곳**
  (추천 9 · 조건부 18 · 근거부족 15 · 제외 3). 4인/6인(부모님) 토글, 근거 링크, 네이버 내비 버튼.
- 사람이 검토하는 표: `docs/PILOT_MISA.md`, 원자료: `data/pilot/misa_places.json`

## 파일럿을 다시 돌리는 순서 (캐시 덕에 대부분 외부 호출 0)
```
python tools/pilot_local.py --max-detail 45 --max-drive 12   # 네이버 발굴·상세, 카카오 검증·별점, 블로그 본문 읽기
python tools/pilot_youtube.py                                # 유튜브 API 근거(캐시 있으면 API 0회, --refresh 로만 재수집)
python tools/pilot_score.py                                  # 게이트·점수·등급 + docs/PILOT_MISA.md
python tools/build_site.py                                   # index.html 재생성
python tools/test_quota_guard.py                             # 안전캡 시험(17개 통과 유지)
```
파이썬 경로: `%LOCALAPPDATA%\Programs\Python\Python312\python.exe` (PATH 미등록). 키는 `.env`(gitignore).

## 이번에 확인·수정한 것 (재발 방지)
- 자동 판독 오판 3건을 직접 읽어 잡음: 부분시설("전시관 문 닫음")·추측("폐업하지 않을까")을 폐업으로, "엘리베이터나 계단"을 계단 주의로 오판.
  → 패턴 수정 + 폐업 제외는 '영업상태 닫힘' 또는 '서로 다른 출처 사실 언급 2건 이상'일 때만. **점수·판정은 반드시 인용문을 읽어 표본 검증할 것.**
- 카카오 별점은 네이버보다 평균 0.8점 낮고 표본이 작다(14쌍 측정) → 네이버 별점이 없을 때만 카카오 +0.8 보정, 기준 0.3 완화, 화면에 표시.
- '영업 중/오늘 휴무'는 수집 시점 스냅샷이라 화면에서 뺐다.

## 남은 일 (우선순위)
1. **폰에서 '네이버 내비 시작' 실기기 테스트** — `nmap://navigation` 딥링크는 데스크톱에서 못 봤다. 안 열리면 '네이버 지도' 웹 링크가 폴백.
2. **모드 B 근거 보강**: 대부분 '좌석 형태(입식/좌식)·층·계단'이 "확인 필요". 블로그를 장소당 2편만 읽었다 → 3~5편으로 늘리거나 사진/메뉴 근거, 전화 확인 안내(현재 카드에 전화번호 표시).
3. `tools/verify.py`(링크·좌표·필수필드·검색URL 금지 검사) 작성 — 게시 전 필수(CLAUDE.md 규칙 6).
4. **당일 나들이 코스 파이프라인**: 유튜브 API 검색 → 영상 설명·댓글로 후보 장소 → 네이버/카카오 검증 → 지역별 코스 조합(식당 1·2·3순위, 카페 1·2순위). 유튜브 자막은 IP 차단(2026-09-24) 해제 확인 후 저속으로만(youtube_scrape 캡 0 → 승인 후 상향).
5. 키즈캠핑 20선, 동네 확대(반경 넓히기·카테고리 추가).
6. 카톡 알림 개편(`send_kakao_alert.py`를 새 데이터 구조로), 주 1회 상태 점검 자동화(Actions).
7. 구글 Places는 보류(사용자 결정 B). 필요해지면 사용자 승인 후 + 체험판 종료일(2026-11-26) 전에만.

## 주의
- 돈이 나가면 안 됨: 모든 외부 호출은 `tools/quota_guard.py` 경유, 유료 API 금지(docs/API_SAFETY.md).
- 이 PC 집 IP가 구글에 캡차 차단됨(유튜브 스크래핑 금지). 네이버는 공개 페이지만 저속·소량.
- 커밋 메시지는 파일로 저장 후 `git commit -F`.
